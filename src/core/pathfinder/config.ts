import { GridTypeNames, SearchAlgoNames } from './../../types'

export const defaultGrid: GridTypeNames = 'triangle'
export const defaultSearchAlgo: SearchAlgoNames = 'depth-first'
export const defaultDelay = 80
export const delayConfig = {
  minValue: 20,
  maxValue: 180,
  step: 40,
}
